import MobileWrapper from "@/layouts/MobileWrapper";
import React from "react";

type Props = {};

const Dashboard = (props: Props) => {
  return <MobileWrapper>Dashboard</MobileWrapper>;
};

export default Dashboard;
