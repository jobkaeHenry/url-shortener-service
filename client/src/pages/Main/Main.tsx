import MobileWrapper from "@/layouts/MobileWrapper";
import React from "react";

type Props = {};

const Main = (props: Props) => {
  return <MobileWrapper>Main</MobileWrapper>;
};

export default Main;
